# Extra Python deps for your benchmarks (torch + CUDA are already in the base
# image — see docker/Dockerfile). Add your inference-engine / bench deps here,
# then: make docker-build && make docker-push
numpy
vllm>=0.8
